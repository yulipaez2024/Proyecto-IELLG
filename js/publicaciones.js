const reaccionIcons = document.querySelectorAll(".imagen-reacciones");
let reaccionCount = 0;

reaccionIcons.forEach((icon) => {
  icon.addEventListener("click", () => {
    reaccionCount++;
    // Actualiza el contador de reacciones
    // Puedes hacer lo que necesites con reaccionCount aquí
  });

  icon.addEventListener("dblclick", () => {
    icon.style.color = "red"; // Cambia el color al hacer doble clic
  });
});

document.addEventListener("DOMContentLoaded", function () {
  var heart1Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart1":
          heart1Count++;
          document.getElementById("heart1-count").textContent = heart1Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart1Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart2Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart2":
          heart2Count++;
          document.getElementById("heart2-count").textContent = heart2Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart2Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart3Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart3":
          heart3Count++;
          document.getElementById("heart3-count").textContent = heart3Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart3Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart4Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart4":
          heart4Count++;
          document.getElementById("heart4-count").textContent = heart4Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart4Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart5Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart5":
          heart5Count++;
          document.getElementById("heart5-count").textContent = heart5Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart5Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart6Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart6":
          heart6Count++;
          document.getElementById("heart6-count").textContent = heart6Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart6Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart7Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart7":
          heart7Count++;
          document.getElementById("heart7-count").textContent = heart7Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart7Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart8Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart8":
          heart8Count++;
          document.getElementById("heart8-count").textContent = heart8Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart8Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart9Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart9":
          heart9Count++;
          document.getElementById("heart9-count").textContent = heart9Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart9Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart10Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart10":
          heart10Count++;
          document.getElementById("heart10-count").textContent = heart10Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart10Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});

document.addEventListener("DOMContentLoaded", function () {
  var heart11Count = 0;

  // Escuchar clics en los iconos de corazón
  var heartIcons = document.querySelectorAll(".heart-icon");
  heartIcons.forEach(function (icon) {
    icon.addEventListener("dblclick", function () {
      var iconId = icon.getAttribute("id");
      switch (iconId) {
        case "heart11":
          heart11Count++;
          document.getElementById("heart11-count").textContent = heart11Count;
          updateTotalCount();
          break;
        // Agregar casos para otros iconos de reacción si es necesario
      }
    });
  });

  function updateTotalCount() {
    var totalHeartCount = heart11Count; // Agregar otros contadores si es necesario
    document.getElementById("total-heart-count").textContent = totalHeartCount;
  }
});
/*CARRUSEL DE BANDA MUSICO MARCIAL*/
document.addEventListener('DOMContentLoaded', function () {
    var carousel = document.getElementById('theCarousel1');
    var videos = carousel.querySelectorAll('video');

    // Agregar evento para pausar otros videos al hacer clic en un video
    videos.forEach(function (video, index) {
        video.addEventListener('play', function () {
            videos.forEach(function (v, i) {
                if (i !== index) {
                    v.pause();
                }
            });
        });
    });

});